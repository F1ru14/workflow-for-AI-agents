# Agent Workflows

This folder holds workflow definitions that outline structured, step-by-step processes for the AI agent to follow.
- `startcycle.md`: The main pipeline sequence to begin processing a new idea through the whole software development lifecycle.
